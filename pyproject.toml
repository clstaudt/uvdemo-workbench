[project]
name = "uvtest-workbench"
version = "0.1.0"
description = "Development workbench for uvtest"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "uvtest",
    "ipykernel>=6.29.0",
]

[tool.uv.sources]
uvtest = { path = "../uvtest", editable = true }
